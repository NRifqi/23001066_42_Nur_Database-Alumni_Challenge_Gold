<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

</head>
<body>

    <%- include("header") %>
    <div class="container mt-5">
    <h3 class="text-center">Data Alumni</h3><br>
        <table class="table">
            <thead>
                <tr>                    
                    <th scope="col">Nama</th>
                    <th scope="col">NIS</th>
                    <th scope="col">Alamat</th>
                    <th scope="col">Tahun Lulus</th>
                    <th scope="col">Sunting / Hapus</th>
                </tr>
            </thead>
                <tbody>
                    <% if( alumni.length === 0 ) { %>
                        <p>data alumni tidak tersedia</p>
                    <%} else {%>
                        <% alumni.forEach((item) => { %>
                        <tr>
                            <td><a href="/detail-alumni/<%= item.id %>"><%= item.nama %></a></td>
                            <td><%= item.nis %></td>
                            <td><%= item.alamat %></td>
                            <td><%= item.angkatan %></td>
                            <td>
                                <button style="background-color: white; border-color: white ;">
                                    <a style="color: black;" href="/edit-alumni/<%= item.id %>">Sunting</a>
                                </button>
                                <button style="background-color: red; border-color: red;" >
                                    <a style="color: white;" href="/delete-alumni/<%= item.id %>">Hapus</a>
                                </button>
                            </td>
                        </tr>
                        <% }) %>
                    <% } %>  
                </tbody> 
         </table>         
     </div>       
    <%- include("footer") %>
    
</body>
</html>